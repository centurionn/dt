
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ТекДата = ТекущаяДата();
	Если Запись.ДатаКомментария <> ТекДата Тогда
		Запись.ДатаКомментария = ТекДата;
	КонецЕсли;	
	
КонецПроцедуры
