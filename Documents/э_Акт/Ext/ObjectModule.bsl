
Процедура ОбработкаПроведения(Отказ, Режим)
	//{{__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!

	// регистр э_АктыЭтран
	//Движения.э_АктыЭтран.Записывать = Истина;
	//Для Каждого ТекСтрокаНакладные Из Накладные Цикл
	//	
	//	СтрокаСФ = СчетФактуры[ТекСтрокаНакладные.НомерСчетФактуры-1];
	//	СтрокаУслуги = Услуги[ТекСтрокаНакладные.НомерУслуги-1];
	//	
	//	Движение = Движения.э_АктыЭтран.Добавить();
	//	Движение.НомерАкта = НомерАкта;
	//	Движение.ИД = ИД;
	//	Движение.НаименованиеУслуги = Наименование;
	//	Движение.ВесГруза = ТекСтрокаНакладные.ВесГруза;
	//	Движение.ВидОтправки = ТекСтрокаНакладные.ВидОтправки;
	//	Движение.ВсегоВалюта = ТекСтрокаНакладные.ВсегоВалюта;
	//	Движение.ВсегоНДСРубли = ТекСтрокаНакладные.ВсегоНДСРубли;
	//	Движение.ВсегоРублей = ТекСтрокаНакладные.ВсегоРублей;
	//	Движение.ДатаОтправки = ТекСтрокаНакладные.ДатаОтправки;
	//	Движение.ДатаПрибытия = ТекСтрокаНакладные.ДатаПрибытия;
	//	Движение.ДатаРаскредитования = ТекСтрокаНакладные.ДатаРаскредитования;
	//	Движение.КодВалюты = ТекСтрокаНакладные.КодВалюты;
	//	Движение.НакладнаяИД = ТекСтрокаНакладные.НакладнаяИД;
	//	Движение.НакладнаяНомер = ТекСтрокаНакладные.НакладнаяНомер;
	//	Движение.НакладнаяСтатус = ТекСтрокаНакладные.НакладнаяСтатус;
	//	Движение.Номер = ТекСтрокаНакладные.Номер;
	//	Движение.НомерВагона = ТекСтрокаНакладные.НомерВагона;
	//	Движение.НомерКонтейнера = ТекСтрокаНакладные.НомерКонтейнера;
	//	Движение.НомерСчетФактуры = ТекСтрокаНакладные.НомерСчетФактуры;
	//	//Движение.НомерУслуги = ТекСтрокаНакладные.НомерУслуги;
	//	//Движение.Период = ТекСтрокаНакладные.Период;
	//	Движение.СтавкаНДСВалюта = ТекСтрокаНакладные.СтавкаНДСВалюта;
	//	Движение.СтавкаНДСРубли = ТекСтрокаНакладные.СтавкаНДСРубли;
	//	Движение.СтанцияНазначение = ТекСтрокаНакладные.СтанцияНазначение;
	//	Движение.СтанцияНазначениеКод = ТекСтрокаНакладные.СтанцияНазначениеКод;
	//	Движение.СтанцияОтправления = ТекСтрокаНакладные.СтанцияОтправления;
	//	Движение.СтанцияОтправленияКод = ТекСтрокаНакладные.СтанцияОтправленияКод;
	//	Движение.СуммаНДСВалюта = ТекСтрокаНакладные.СуммаНДСВалюта;
	//	Движение.СуммаНДСРубли = ТекСтрокаНакладные.СуммаНДСРубли;
	//	Движение.Тип = ТекСтрокаНакладные.Тип;
	//	Движение.ТипОтправки = ТекСтрокаНакладные.ТипОтправки;
	//	Движение.УслугаКод = ТекСтрокаНакладные.УслугаКод;
	//	Движение.УслугаНаименование = ТекСтрокаНакладные.УслугаНаименование;
	//	Движение.КодГНГиЕТСНГ = ТекСтрокаНакладные.КодГНГиЕТСНГ;
	//КонецЦикла;

	//}}__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
КонецПроцедуры
