
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	МассивМаркеров = Новый Массив;
	//НСтруктура = Новый Структура("Широта, Долгота, Подсказка", "59.900857", "30.288649", "SeaData<br/>+7 (812) 380-38-28");
	//МассивМаркеров.Добавить(НСтруктура);
	МассивЛиний = Новый Массив;
	ПараметрыФормы = Новый Структура("Маркеры, Линии", МассивМаркеров, МассивЛиний);
	ОткрытьФорму("ОбщаяФорма.Геосервис", ПараметрыФормы, , ИСТИНА);
	
КонецПроцедуры
